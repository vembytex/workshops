## Installation

```sh
python3 -m venv apivenv

pip install flask ariadne flask-sqlalchemy flask-cors jsonrpcserver
```

To start the server

```sh
./start.sh
```

- To explore the graphql playground access `/graphql`
- RPC router listening on `/rpc` (POST)
